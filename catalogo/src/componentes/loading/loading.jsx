import './loading.css'; // Importamos el archivo CSS para estilos

const LoadingComponent = () => {
  return (
    <div className="loading-container">
      <div className="loader"></div>
    </div>
  );
};

export default LoadingComponent;
